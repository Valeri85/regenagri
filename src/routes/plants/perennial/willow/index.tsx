import { component$ } from '@builder.io/qwik';

export default component$(() => {
	return (
		<div>
			{/* ტირიფი */}
			<h1>Willow</h1>
			<section aria-label='plant-doc'></section>
		</div>
	);
});
