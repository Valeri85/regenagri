import { component$ } from '@builder.io/qwik';

export default component$(() => {
	return (
		<div>
			{/* სორგო */}
			<h1>Sorghum</h1>
			<section aria-label='plant-doc'></section>
		</div>
	);
});
